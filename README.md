A one-page PyQt6 app that launches into a Flappy Bird play screen (rendered with Pygame).

To run:

```
pip install -r requirements.txt
python flappy_app.py
```

Click "Play" to open the game window. Press space to jump/restart. High score is session-only (resets on app restart).
